# Configuración de generación de localizaciones.
# flutter gen-l10n lee este fichero y produce AppLocalizations a partir
# de los ARB de lib/l10n, tomando app_es.arb como plantilla de claves.

arb-dir: lib/l10n
template-arb-file: app_es.arb
output-localization-file: app_localizations.dart
output-class: AppLocalizations
untranslated-messages-file: l10n_missing.txt
nullable-getter: false
# Flutter 3.40+ desactivó el paquete sintético `flutter_gen` por defecto.
# Generamos los `app_localizations*.dart` físicamente en `lib/l10n/` y
# los importamos por ruta del proyecto (`package:flavor_news_hub/...`).
output-dir: lib/l10n
